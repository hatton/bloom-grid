// src/index.ts
export function attach(rootDiv: HTMLDivElement): void {
  console.log('Attaching BloomGrid to', rootDiv);
  // Implementation to follow
}

export function detach(rootDiv: HTMLDivElement): void {
  console.log('Detaching BloomGrid from', rootDiv);
  // Implementation to follow
}
